import { DatePerRegionPipe } from './date-per-region-pipe';

describe('DatePerRegionPipe', () => {
  it('create an instance', () => {
    const pipe = new DatePerRegionPipe();
    expect(pipe).toBeTruthy();
  });
});
